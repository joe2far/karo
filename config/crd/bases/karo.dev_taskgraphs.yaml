---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.15.0
  name: taskgraphs.karo.dev
spec:
  group: karo.dev
  names:
    kind: TaskGraph
    listKind: TaskGraphList
    plural: taskgraphs
    singular: taskgraph
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.totalTasks
      name: Tasks
      type: integer
    - jsonPath: .status.closedTasks
      name: Completed
      type: integer
    - jsonPath: .status.completionPercent
      name: Completion%
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: TaskGraphSpec defines the desired state of TaskGraph
            properties:
              description:
                type: string
              dispatchPolicy:
                properties:
                  allowAgentMutation:
                    type: boolean
                  defaultTimeoutMinutes:
                    format: int32
                    minimum: 1
                    type: integer
                  maxConcurrent:
                    format: int32
                    minimum: 0
                    type: integer
                  retryPolicy:
                    properties:
                      backoffSeconds:
                        format: int32
                        minimum: 0
                        type: integer
                      maxRetries:
                        format: int32
                        minimum: 0
                        type: integer
                      onExhaustion:
                        enum:
                        - EscalateToHuman
                        - Fail
                        - Reopen
                        type: string
                    required:
                    - backoffSeconds
                    - maxRetries
                    - onExhaustion
                    type: object
                required:
                - allowAgentMutation
                - maxConcurrent
                - retryPolicy
                type: object
              dispatcherRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      TODO: Add other useful fields. apiVersion, kind, uid?
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              ownerAgentRef:
                description: |-
                  LocalObjectReference contains enough information to let you locate the
                  referenced object inside the same namespace.
                properties:
                  name:
                    default: ""
                    description: |-
                      Name of the referent.
                      This field is effectively required, but due to backwards compatibility is
                      allowed to be empty. Instances of this type with an empty value here are
                      almost certainly wrong.
                      TODO: Add other useful fields. apiVersion, kind, uid?
                      More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                      TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                    type: string
                type: object
                x-kubernetes-map-type: atomic
              tasks:
                items:
                  properties:
                    acceptanceCriteria:
                      items:
                        type: string
                      type: array
                    addedAt:
                      format: date-time
                      type: string
                    addedBy:
                      type: string
                    deps:
                      items:
                        type: string
                      type: array
                    description:
                      type: string
                    evalGate:
                      properties:
                        evalSuiteRef:
                          description: |-
                            LocalObjectReference contains enough information to let you locate the
                            referenced object inside the same namespace.
                          properties:
                            name:
                              default: ""
                              description: |-
                                Name of the referent.
                                This field is effectively required, but due to backwards compatibility is
                                allowed to be empty. Instances of this type with an empty value here are
                                almost certainly wrong.
                                TODO: Add other useful fields. apiVersion, kind, uid?
                                More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                              type: string
                          type: object
                          x-kubernetes-map-type: atomic
                        minPassRate:
                          maximum: 1
                          minimum: 0
                          type: number
                        onFail:
                          enum:
                          - Reopen
                          - Escalate
                          type: string
                      required:
                      - evalSuiteRef
                      - minPassRate
                      - onFail
                      type: object
                      x-kubernetes-validations:
                      - message: minPassRate must be between 0.0 and 1.0
                        rule: self.minPassRate >= 0.0 && self.minPassRate <= 1.0
                    id:
                      maxLength: 253
                      minLength: 1
                      pattern: ^[a-z0-9]([a-z0-9\-]*[a-z0-9])?$
                      type: string
                    priority:
                      enum:
                      - High
                      - Medium
                      - Low
                      type: string
                    sandboxClassOverride:
                      description: |-
                        LocalObjectReference contains enough information to let you locate the
                        referenced object inside the same namespace.
                      properties:
                        name:
                          default: ""
                          description: |-
                            Name of the referent.
                            This field is effectively required, but due to backwards compatibility is
                            allowed to be empty. Instances of this type with an empty value here are
                            almost certainly wrong.
                            TODO: Add other useful fields. apiVersion, kind, uid?
                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                            TODO: Drop `kubebuilder:default` when controller-gen doesn't need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.
                          type: string
                      type: object
                      x-kubernetes-map-type: atomic
                    timeoutMinutes:
                      format: int32
                      minimum: 1
                      type: integer
                    title:
                      minLength: 1
                      type: string
                    type:
                      enum:
                      - design
                      - impl
                      - eval
                      - review
                      - infra
                      - approval
                      type: string
                  required:
                  - addedAt
                  - addedBy
                  - deps
                  - id
                  - priority
                  - title
                  - type
                  type: object
                  x-kubernetes-validations:
                  - message: task cannot depend on itself
                    rule: '!self.deps.exists(d, d == self.id)'
                minItems: 1
                type: array
            required:
            - dispatchPolicy
            - dispatcherRef
            - ownerAgentRef
            - tasks
            type: object
          status:
            description: TaskGraphStatus defines the observed state of TaskGraph
            properties:
              blockedTasks:
                format: int32
                type: integer
              closedTasks:
                format: int32
                type: integer
              completionPercent:
                format: int32
                type: integer
              conditions:
                items:
                  description: "Condition contains details for one aspect of the current
                    state of this API Resource.\n---\nThis struct is intended for
                    direct use as an array at the field path .status.conditions.  For
                    example,\n\n\n\ttype FooStatus struct{\n\t    // Represents the
                    observations of a foo's current state.\n\t    // Known .status.conditions.type
                    are: \"Available\", \"Progressing\", and \"Degraded\"\n\t    //
                    +patchMergeKey=type\n\t    // +patchStrategy=merge\n\t    // +listType=map\n\t
                    \   // +listMapKey=type\n\t    Conditions []metav1.Condition `json:\"conditions,omitempty\"
                    patchStrategy:\"merge\" patchMergeKey:\"type\" protobuf:\"bytes,1,rep,name=conditions\"`\n\n\n\t
                    \   // other fields\n\t}"
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: |-
                        type of condition in CamelCase or in foo.example.com/CamelCase.
                        ---
                        Many .condition.type values are consistent across resources like Available, but because arbitrary conditions can be
                        useful (see .node.status.conditions), the ability to deconflict is important.
                        The regex it matches is (dns1123SubdomainFmt/)?(qualifiedNameFmt)
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
              dispatchedTasks:
                format: int32
                type: integer
              evalPendingTasks:
                format: int32
                type: integer
              failedTasks:
                format: int32
                type: integer
              inProgressTasks:
                format: int32
                type: integer
              lastDispatchedAt:
                format: date-time
                type: string
              openTasks:
                format: int32
                type: integer
              phase:
                type: string
              taskStatuses:
                additionalProperties:
                  properties:
                    assignedAt:
                      format: date-time
                      type: string
                    assignedTo:
                      type: string
                    completedAt:
                      format: date-time
                      type: string
                    evalResult:
                      properties:
                        evaluatedAt:
                          format: date-time
                          type: string
                        failureNotes:
                          type: string
                        passRate:
                          maximum: 1
                          minimum: 0
                          type: number
                        passed:
                          type: boolean
                      required:
                      - evaluatedAt
                      - passRate
                      - passed
                      type: object
                    failureNotes:
                      type: string
                    phase:
                      type: string
                    resultArtifactRef:
                      type: string
                    retryCount:
                      format: int32
                      type: integer
                    startedAt:
                      format: date-time
                      type: string
                  required:
                  - phase
                  - retryCount
                  type: object
                type: object
              totalTasks:
                format: int32
                type: integer
            required:
            - blockedTasks
            - closedTasks
            - completionPercent
            - dispatchedTasks
            - evalPendingTasks
            - failedTasks
            - inProgressTasks
            - openTasks
            - phase
            - totalTasks
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
